# Dev tooling installed alongside the base packages, via conda only (conda-forge channel).
# Pinned so local devs and CI see identical lint/type-check results.
# mypy pinned to match the version already deployed in gis-env; ruff is new, pinned to
# the latest available on conda-forge as of 2026-08-26.
ruff==0.16.4
mypy==1.2.0
