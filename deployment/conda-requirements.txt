# Base packages installed on top of ArcGIS Pro's arcgispro-py3 clone.
# Installed via: conda install -n gis-env --file conda-requirements.txt --file conda-requirements-dev.txt
# Left unpinned so conda resolves versions compatible with the cloned base env.
# Kept separate from conda-requirements-dev.txt so CI's lint job (ruff/mypy/PSScriptAnalyzer)
# can skip installing these -- it never imports geopandas/fiona/numpy.
geopandas
numpy
fiona
# Declares existing dependencies rather than adding them -- listed so a future base that
# drops either fails loudly at deploy time instead of silently at run time.
requests
beautifulsoup4
